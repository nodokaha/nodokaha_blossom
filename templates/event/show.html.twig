{% extends 'basisvr_base.html.twig' %}

{% block title %}{{ post.title }} — BasisVR コンテンツ投稿{% endblock %}

{% block body %}
<main id="basisvr-main" class="blog-page blog-article-page">
  <nav class="breadcrumb" aria-label="パンくずリスト">
    <a href="{{ path('basisvr_event_index') }}">コンテンツ一覧</a>
    <span aria-hidden="true">/</span>
    <span>{{ post.title }}</span>
  </nav>

  <section class="article-hero">
    <div>
      <p class="eyebrow">BasisVR Content</p>
      <h1>{{ post.title }}</h1>
      <p class="article-meta">{{ post.contentTypeLabel }} · {{ post.authorName }} · <time datetime="{{ post.publishedAt|date('c') }}">{{ post.publishedAt|date('Y.m.d H:i') }}</time></p>
      {% if post.tags is not empty %}
        <div class="post-tags" aria-label="タグ">
          {% for tag in post.tags %}<span>#{{ tag }}</span>{% endfor %}
        </div>
      {% endif %}
    </div>
    <div class="article-actions" aria-label="コンテンツ投稿アクション">
      <a class="ghost-button" href="{{ path('basisvr_event_index') }}">一覧へ戻る</a>
      <a class="blog-button" href="#comment-form">コメントする</a>
    </div>
  </section>

  <article class="post-content card">
    <h2>説明</h2>
    <div class="content-body">{{ post.description|nl2br }}</div>
    {% if post.relatedAssets is not empty %}
      <section class="related-assets" aria-labelledby="related-assets-heading">
        <h3 id="related-assets-heading">関連アセット</h3>
        <ul>
          {% for asset in post.relatedAssets %}
            <li>{{ asset }}</li>
          {% endfor %}
        </ul>
      </section>
    {% endif %}
  </article>

  <section id="comments" class="comments-section" aria-labelledby="comments-heading">
    <div class="section-header">
      <h2 id="comments-heading">コメント</h2>
      <span class="comment-count">{{ post.comments|length }}件</span>
    </div>
    <div class="comment-thread">
      {% for c in post.comments %}
        <article class="comment-card">
          <div class="comment-head">
            <strong>{{ c.authorName }}</strong>
            <time datetime="{{ c.publishedAt|date('c') }}">{{ c.publishedAt|date('Y.m.d H:i') }}</time>
          </div>
          <div class="comment-body">{{ c.content|nl2br }}</div>
        </article>
      {% else %}
        <div class="empty-state card">
          <h3>まだコメントはありません</h3>
          <p>利用時の気づき、導入手順の補足、改善案を残せます。</p>
        </div>
      {% endfor %}
    </div>
  </section>

  <section id="comment-form" class="comment-form" aria-labelledby="comment-form-heading">
    <div class="section-header">
      <h2 id="comment-form-heading">コメントを書く</h2>
      <p>使い方の補足や導入時の気づきを短く共有できます。</p>
    </div>
    <div class="card form-card">
      {{ form_start(commentForm, {'attr': {'class': 'blog-form'}}) }}
        {{ form_errors(commentForm) }}
        {{ form_row(commentForm.authorName) }}
        {{ form_row(commentForm.content) }}
        {{ form_row(commentForm.website) }}
        {{ form_row(commentForm.submissionToken) }}
        <div class="form-actions">
          <button class="blog-button" type="submit">コメントを投稿</button>
          <a class="ghost-button" href="{{ path('basisvr_event_index') }}">一覧へ戻る</a>
        </div>
      {{ form_end(commentForm) }}
    </div>
  </section>
</main>
{% endblock %}
